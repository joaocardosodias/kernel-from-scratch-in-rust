[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
spin = "0.12.0"

[profile.release]
panic = "abort"
lto = true
debug = false
opt-level = "z"
codegen-units = 1

[profile.dev]
panic = "abort"
