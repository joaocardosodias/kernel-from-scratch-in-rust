[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]

[profile.release]
panic = "abort"
lto = true
debug = false
opt-level = "z"
codegen-units = 1

[profile.dev]
panic = "abort"
